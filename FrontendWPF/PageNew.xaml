<Page x:Class="WpfApp1.PageNew"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:WpfApp1"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="Newsave">

    <!-- Les ressources de la Page -->
    <Page.Resources>
        <Style x:Key="TextBoxStyle" TargetType="TextBox">
            <Setter Property="Margin" Value="5"/>
            <Setter Property="Padding" Value="3"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="BorderBrush" Value="#FFABADB3"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border CornerRadius="5" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}">
                            <ScrollViewer x:Name="PART_ContentHost"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="ButtonStyle" TargetType="Button">
            <Setter Property="Margin" Value="5"/>
            <Setter Property="Padding" Value="5"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="BorderBrush" Value="#FFABADB3"/>
            <Setter Property="Background" Value="#FFDDDDDD"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border CornerRadius="5" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" Background="{TemplateBinding Background}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Page.Resources>

    <!-- Contenu de la Page -->
    <Grid Background="White">
        <GroupBox Header="Nouvelle sauvegarde" Margin="10">
            <StackPanel>
                <!-- Grille pour le nom de la sauvegarde et le type de sauvegarde -->
                <Grid Margin="5">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <!-- Nom de la sauvegarde -->
                    <StackPanel Grid.Column="0">
                        <TextBlock Text="Nom de votre sauvegarde" Margin="5"/>
                        <TextBox Style="{StaticResource TextBoxStyle}" Height="25" Width="300"/>
                    </StackPanel>

                    <!-- Type de la sauvegarde -->
                    <StackPanel Grid.Column="1" Margin="5,0,0,0">
                        <TextBlock Text="Type de sauvegarde" Margin="5"/>
                        <ComboBox Height="25" Width="300" Margin="5" SelectionChanged="ComboBox_SelectionChanged">
                            <ComboBoxItem Content="Totale"/>
                            <ComboBoxItem Content="Différentielle"/>
                        </ComboBox>
                    </StackPanel>
                </Grid>

                <!-- Répertoire source -->
                <TextBlock Text="Répertoire source" Margin="5"/>
                <StackPanel Orientation="Horizontal" Margin="5">
                    <TextBox Style="{StaticResource TextBoxStyle}" Height="25" Width="200"/>
                    <Button Content="Parcourir" Style="{StaticResource ButtonStyle}" Width="100"/>
                </StackPanel>

                <!-- Répertoire destination -->
                <TextBlock Text="Répertoire destination" Margin="5"/>
                <StackPanel Orientation="Horizontal" Margin="5">
                    <TextBox Style="{StaticResource TextBoxStyle}" Height="25" Width="200"/>
                    <Button Content="Parcourir" Style="{StaticResource ButtonStyle}" Width="100"/>
                </StackPanel>

                <!-- Bouton pour créer la sauvegarde -->
                <Button Content="Créer la sauvegarde" Style="{StaticResource ButtonStyle}" Height="30" Width="200" Margin="5"/>
            </StackPanel>
        </GroupBox>
    </Grid>

</Page>
